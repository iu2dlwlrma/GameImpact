<Window x:Class="GameImpact.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GameImpact" Height="600" Width="900"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">
    
    <Border x:Name="RootBorder" Background="{DynamicResource AppBackground}" CornerRadius="8" BorderBrush="{DynamicResource AppBorder}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 自定义标题栏 -->
            <Border Grid.Row="0" Background="{DynamicResource AppSurface}" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 左侧：Logo + 标题 -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="◆" FontSize="16" Foreground="{DynamicResource AppAccent}" VerticalAlignment="Center"/>
                        <TextBlock Text="GameImpact" FontSize="13" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource AppText}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- 中间：主要操作 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- 窗口选择 -->
                        <Border Background="{DynamicResource AppSurfaceVariant}" CornerRadius="6" Padding="3" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding WindowDisplayText}" 
                                           Foreground="{DynamicResource AppTextSecondary}" 
                                           VerticalAlignment="Center" Margin="10,0,6,0" FontSize="12"
                                           MaxWidth="160" TextTrimming="CharacterEllipsis"/>
                                <Button Content="选择" Style="{DynamicResource SubtleButton}" 
                                        Padding="10,4" FontSize="11"
                                        Command="{Binding SelectWindowCommand}"/>
                            </StackPanel>
                        </Border>

                        <!-- GPU HDR -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                            <TextBlock Text="GPU" Foreground="{DynamicResource AppTextMuted}" 
                                       FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <CheckBox IsChecked="{Binding UseGpuHdrConversion}" VerticalAlignment="Center"
                                      ToolTip="GPU HDR 转换"/>
                        </StackPanel>

                        <!-- 启动/停止按钮 -->
                        <Button Style="{DynamicResource AccentButton}" 
                                Padding="16,6" FontSize="12"
                                Command="{Binding ToggleCaptureCommand}"
                                Content="{Binding CaptureButtonText}"/>
                    </StackPanel>

                    <!-- 右侧：工具按钮 -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,4,0">
                        <Button x:Name="DebugBtn" Style="{DynamicResource IconButton}" ToolTip="调试面板" Click="DebugPanel_Click">
                            <TextBlock Text="🔧" FontSize="14"/>
                        </Button>
                        <Button Style="{DynamicResource IconButton}" ToolTip="切换主题" Click="ThemeToggle_Click">
                            <TextBlock x:Name="ThemeIcon" Text="🌙" FontSize="14"/>
                        </Button>
                        <Button Style="{DynamicResource IconButton}" ToolTip="设置" Click="Settings_Click">
                            <TextBlock Text="⚙" FontSize="14"/>
                        </Button>
                    </StackPanel>
                    
                    <!-- 窗口控制按钮 -->
                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Stretch">
                        <Button Style="{DynamicResource WindowButton}" Click="Minimize_Click" ToolTip="最小化">
                            <TextBlock Text="─" FontSize="12" Margin="0,-2,0,0"/>
                        </Button>
                        <Button Style="{DynamicResource WindowButton}" Click="Maximize_Click" ToolTip="最大化">
                            <TextBlock x:Name="MaximizeIcon" Text="□" FontSize="12"/>
                        </Button>
                        <Button Style="{DynamicResource WindowCloseButton}" Click="Close_Click" ToolTip="关闭">
                            <TextBlock Text="✕" FontSize="11"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 主内容区 -->
            <Grid Grid.Row="1" Margin="16,12,16,12">
                <!-- 默认状态视图 -->
                <Border x:Name="DefaultView" Background="{DynamicResource AppSurface}" CornerRadius="10" 
                        BorderBrush="{DynamicResource AppBorderSubtle}" BorderThickness="1">
                    <Grid>
                        <!-- 空状态提示 -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Visibility="{Binding IsIdle, Converter={StaticResource BoolToVisibility}}">
                            <TextBlock Text="◇" FontSize="42" Foreground="{DynamicResource AppBorder}" 
                                       HorizontalAlignment="Center" Margin="0,0,0,14"/>
                            <TextBlock Text="选择窗口并启动捕获" 
                                       FontSize="13" Foreground="{DynamicResource AppTextMuted}"
                                       HorizontalAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- 运行中状态 -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                                    Visibility="{Binding IsCapturing, Converter={StaticResource BoolToVisibility}}">
                            <TextBlock Text="●" FontSize="20" Foreground="{DynamicResource AppSuccess}" 
                                       HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="捕获运行中" 
                                       FontSize="13" Foreground="{DynamicResource AppTextSecondary}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding StatusText}" 
                                       FontSize="11" Foreground="{DynamicResource AppTextMuted}"
                                       HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 调试面板视图 -->
                <Border x:Name="DebugView" Visibility="Collapsed">
                    <TabControl Background="Transparent" BorderThickness="0">
                        <TabControl.Resources>
                            <Style TargetType="TabItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{DynamicResource AppTextSecondary}"/>
                                <Setter Property="Padding" Value="14,8"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TabItem">
                                            <Border x:Name="Border" Background="{TemplateBinding Background}" 
                                                    CornerRadius="6,6,0,0" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource AppText}"/>
                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource AppSurface}"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource AppSurfaceVariant}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TabControl.Resources>
                        
                        <!-- 日志 Tab -->
                        <TabItem Header="📋 日志">
                            <Border Background="{DynamicResource AppSurface}" CornerRadius="0,6,6,6" 
                                    BorderBrush="{DynamicResource AppBorderSubtle}" BorderThickness="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <Border Grid.Row="0" BorderBrush="{DynamicResource AppBorderSubtle}" 
                                            BorderThickness="0,0,0,1" Padding="10,6">
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="清空" Style="{DynamicResource SubtleButton}" 
                                                    Padding="10,4" FontSize="11" Click="ClearLog_Click"/>
                                            <CheckBox Content="自动滚动" IsChecked="{Binding AutoScrollLog}" 
                                                      Margin="14,0,0,0" VerticalAlignment="Center" FontSize="11"
                                                      Foreground="{DynamicResource AppTextSecondary}"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
                                        <TextBlock Text="{Binding LogText}" 
                                                   FontFamily="Cascadia Code,Consolas,monospace" 
                                                   Foreground="{DynamicResource AppTextSecondary}" 
                                                   FontSize="11" TextWrapping="Wrap"/>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                        </TabItem>
                        
                        <!-- 预览 Tab -->
                        <TabItem Header="🖼 预览">
                            <Border Background="{DynamicResource AppSurface}" CornerRadius="0,6,6,6" 
                                    BorderBrush="{DynamicResource AppBorderSubtle}" BorderThickness="1">
                                <Grid Margin="14">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="分辨率:" Foreground="{DynamicResource AppTextMuted}" 
                                                   VerticalAlignment="Center" Margin="0,0,6,0" FontSize="11"/>
                                        <TextBlock Text="{Binding PreviewResolution}" 
                                                   Foreground="{DynamicResource AppTextSecondary}" FontSize="11"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="FPS:" Foreground="{DynamicResource AppTextMuted}" 
                                                   VerticalAlignment="Center" Margin="20,0,6,0" FontSize="11"/>
                                        <TextBlock Text="{Binding PreviewFps}" 
                                                   Foreground="{DynamicResource AppAccent}" FontSize="11"
                                                   VerticalAlignment="Center" FontWeight="Medium"/>
                                    </StackPanel>
                                    
                                    <Border Grid.Row="1" Background="{DynamicResource AppBackground}" 
                                            CornerRadius="6" ClipToBounds="True">
                                        <Image Source="{Binding PreviewSource}" Stretch="Uniform" 
                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
                                        <CheckBox Content="启用预览" IsChecked="{Binding EnablePreview}" FontSize="11"
                                                  Foreground="{DynamicResource AppTextSecondary}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </TabItem>
                        
                        <!-- 坐标测试 Tab -->
                        <TabItem Header="🎯 坐标">
                            <Border Background="{DynamicResource AppSurface}" CornerRadius="0,6,6,6" 
                                    BorderBrush="{DynamicResource AppBorderSubtle}" BorderThickness="1">
                                <Grid Margin="14">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    
                                    <!-- 坐标输入 -->
                                    <Border Grid.Row="0" Background="{DynamicResource AppSurfaceVariant}" 
                                            CornerRadius="6" Padding="12" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                <TextBlock Text="X:" Foreground="{DynamicResource AppTextMuted}" 
                                                           VerticalAlignment="Center" FontSize="11"/>
                                                <TextBox x:Name="MouseX" Width="60" Margin="4,0,12,0" Text="100" FontSize="11"/>
                                                <TextBlock Text="Y:" Foreground="{DynamicResource AppTextMuted}" 
                                                           VerticalAlignment="Center" FontSize="11"/>
                                                <TextBox x:Name="MouseY" Width="60" Margin="4,0,12,0" Text="100" FontSize="11"/>
                                                <Button Content="🎯 拾取" Style="{DynamicResource AccentButton}" 
                                                        Padding="12,4" FontSize="11" Click="PickCoord_Click"
                                                        ToolTip="在目标窗口上拾取坐标"/>
                                            </StackPanel>
                                            
                                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                                <Button Content="点击" Style="{DynamicResource SubtleButton}" 
                                                        Padding="12,4" FontSize="11" Click="TestMouseClick_Click"/>
                                                <Button Content="移动" Style="{DynamicResource SubtleButton}" 
                                                        Padding="12,4" FontSize="11" Margin="6,0,0,0" Click="TestMouseMove_Click"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- OCR 识别 -->
                                    <Border Grid.Row="1" Background="{DynamicResource AppSurfaceVariant}" 
                                            CornerRadius="6" Padding="12" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                                <TextBlock Text="OCR" FontWeight="SemiBold" FontSize="12"
                                                           Foreground="{DynamicResource AppText}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            
                                            <TextBox x:Name="OcrSearchText" Grid.Column="1" Margin="12,0" FontSize="11"
                                                     VerticalAlignment="Center"
                                                     ToolTip="输入要查找的文本，留空则识别全屏"/>
                                            
                                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                                <Button Content="查找" Style="{DynamicResource AccentButton}" 
                                                        Padding="12,4" FontSize="11" Click="TestOcrFind_Click"
                                                        ToolTip="在窗口中查找文本并返回坐标"/>
                                                <Button Content="全屏识别" Style="{DynamicResource SubtleButton}" 
                                                        Padding="12,4" FontSize="11" Margin="6,0,0,0" Click="TestOcrFull_Click"
                                                        ToolTip="识别整个窗口的文字"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- 键盘测试 -->
                                    <Border Grid.Row="2" Background="{DynamicResource AppSurfaceVariant}" 
                                            CornerRadius="6" Padding="12" Margin="0,0,0,10">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="按键:" Foreground="{DynamicResource AppTextMuted}" 
                                                       VerticalAlignment="Center" FontSize="11"/>
                                            <TextBox x:Name="KeyInput" Width="80" Margin="4,0,12,0" Text="A" FontSize="11"/>
                                            <Button Content="发送" Style="{DynamicResource AccentButton}" 
                                                    Padding="12,4" FontSize="11" Click="TestKeyPress_Click"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- 测试日志 -->
                                    <Border Grid.Row="3" Background="{DynamicResource AppBackground}" 
                                            CornerRadius="6" Padding="10">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <TextBlock x:Name="InputTestLog" 
                                                       FontFamily="Cascadia Code,Consolas,monospace" 
                                                       Foreground="{DynamicResource AppTextMuted}" 
                                                       FontSize="10" TextWrapping="Wrap"
                                                       Text="测试日志..."/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Border>
                        </TabItem>
                    </TabControl>
                </Border>
            </Grid>

            <!-- 底部状态栏 -->
            <Border Grid.Row="2" Background="{DynamicResource AppSurface}" CornerRadius="0,0,8,8"
                    BorderBrush="{DynamicResource AppBorderSubtle}" BorderThickness="0,1,0,0"
                    Padding="16,6">
                <TextBlock Text="{Binding StatusMessage}" 
                           Foreground="{DynamicResource AppTextMuted}" FontSize="11"
                           VerticalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Window>
