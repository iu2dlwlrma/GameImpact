<Window x:Class="GameImpact.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="GameImpact" Height="600" Width="900"
    MinHeight="400" MinWidth="600"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="CanResizeWithGrip">

    <Border x:Name="RootBorder" Background="{DynamicResource AppBackground}" CornerRadius="8"
        BorderBrush="{DynamicResource AppBorder}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 自定义标题栏 -->
            <Border Grid.Row="0" Background="{DynamicResource AppSurface}" CornerRadius="8,8,0,0"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- 左侧：Logo + 标题 -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0,0,0"
                        VerticalAlignment="Center">
                        <TextBlock Text="◆" FontSize="16" Foreground="{DynamicResource AppAccent}"
                            VerticalAlignment="Center" />
                        <TextBlock x:Name="TitleText" Text="GameImpact" FontSize="13" FontWeight="SemiBold"
                            Foreground="{DynamicResource AppText}" Margin="8,0,0,0"
                            VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- 中间：主要操作 -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- 窗口选择 -->
                        <Border Background="{DynamicResource AppSurfaceVariant}" CornerRadius="6"
                            Padding="3" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding WindowDisplayText}"
                                    Foreground="{DynamicResource AppTextSecondary}"
                                    VerticalAlignment="Center" Margin="10,0,6,0" FontSize="12"
                                    MaxWidth="160" TextTrimming="CharacterEllipsis" />
                                <Button Content="选择" Style="{DynamicResource SubtleButton}"
                                    Padding="10,4" FontSize="11"
                                    Command="{Binding SelectWindowCommand}" />
                            </StackPanel>
                        </Border>

                        <!-- GPU HDR -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                            <TextBlock Text="GPU" Foreground="{DynamicResource AppTextMuted}"
                                FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0" />
                            <CheckBox IsChecked="{Binding UseGpuHdrConversion}"
                                VerticalAlignment="Center"
                                ToolTip="GPU HDR 转换" />
                        </StackPanel>

                        <!-- 启动/停止按钮 -->
                        <Button Style="{DynamicResource AccentButton}"
                            Padding="16,6" FontSize="12"
                            Command="{Binding ToggleCaptureCommand}"
                            Content="{Binding CaptureButtonText}" />
                    </StackPanel>

                    <!-- 右侧：工具按钮 -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"
                        Margin="0,0,4,0">
                        <Button x:Name="DebugBtn" Style="{DynamicResource IconButton}"
                            ToolTip="调试面板" Click="DebugPanel_Click">
                            <TextBlock Text="🔧" FontSize="14" />
                        </Button>
                        <Button Style="{DynamicResource IconButton}" ToolTip="切换主题"
                            Click="ThemeToggle_Click">
                            <TextBlock x:Name="ThemeIcon" Text="🌙" FontSize="14" />
                        </Button>
                        <Button Style="{DynamicResource IconButton}" ToolTip="设置"
                            Click="Settings_Click">
                            <TextBlock Text="⚙" FontSize="14" />
                        </Button>
                    </StackPanel>

                    <!-- 窗口控制按钮 -->
                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Stretch">
                        <Button Style="{DynamicResource WindowButton}" Click="Minimize_Click"
                            ToolTip="最小化">
                            <TextBlock Text="─" FontSize="12" Margin="0,-2,0,0" />
                        </Button>
                        <Button Style="{DynamicResource WindowButton}" Click="Maximize_Click"
                            ToolTip="最大化">
                            <TextBlock x:Name="MaximizeIcon" Text="□" FontSize="12" />
                        </Button>
                        <Button Style="{DynamicResource WindowCloseButton}" Click="Close_Click"
                            ToolTip="关闭">
                            <TextBlock Text="✕" FontSize="11" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 主内容区 -->
            <Grid Grid.Row="1" Margin="16,12,16,12">
                <!-- 子项目自定义内容插槽，默认隐藏 -->
                <ContentControl x:Name="CustomContentHost" Visibility="Collapsed" />

                <!-- 默认状态视图 -->
                <Border x:Name="DefaultView" Background="{DynamicResource AppSurface}"
                    CornerRadius="10"
                    BorderBrush="{DynamicResource AppBorderSubtle}" BorderThickness="1">
                    <Grid>
                        <!-- 空状态提示 -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding IsIdle, Converter={StaticResource BoolToVisibility}}">
                            <TextBlock Text="◇" FontSize="42"
                                Foreground="{DynamicResource AppBorder}"
                                HorizontalAlignment="Center" Margin="0,0,0,14" />
                            <TextBlock Text="选择窗口并启动捕获"
                                FontSize="13" Foreground="{DynamicResource AppTextMuted}"
                                HorizontalAlignment="Center" />
                        </StackPanel>

                        <!-- 运行中状态 -->
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding IsCapturing, Converter={StaticResource BoolToVisibility}}">
                            <TextBlock Text="●" FontSize="20"
                                Foreground="{DynamicResource AppSuccess}"
                                HorizontalAlignment="Center" Margin="0,0,0,10" />
                            <TextBlock Text="捕获运行中"
                                FontSize="13" Foreground="{DynamicResource AppTextSecondary}"
                                HorizontalAlignment="Center" />
                            <TextBlock Text="{Binding StatusText}"
                                FontSize="11" Foreground="{DynamicResource AppTextMuted}"
                                HorizontalAlignment="Center" Margin="0,4,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>


            </Grid>

            <!-- 底部状态栏 -->
            <Border Grid.Row="2" Background="{DynamicResource AppSurface}" CornerRadius="0,0,8,8"
                BorderBrush="{DynamicResource AppBorderSubtle}" BorderThickness="0,1,0,0"
                Padding="16,6">
                <TextBlock Text="{Binding StatusMessage}"
                    Foreground="{DynamicResource AppTextMuted}" FontSize="11"
                    VerticalAlignment="Center" />
            </Border>
        </Grid>
    </Border>
</Window>