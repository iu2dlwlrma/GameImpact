<Window x:Class="GameImpact.UI.Views.WindowSelectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择窗口" Height="450" Width="550"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Border Background="{DynamicResource AppBackground}" CornerRadius="8"
            BorderBrush="{DynamicResource AppBorder}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="{DynamicResource AppSurface}" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="选择窗口" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource AppText}"
                               VerticalAlignment="Center" Margin="14,0,0,0" />
                    <Button Style="{DynamicResource WindowCloseButton}"
                            HorizontalAlignment="Right" Width="40" Height="40"
                            Click="CancelButton_Click">
                        <TextBlock Text="✕" FontSize="10" />
                    </Button>
                </Grid>
            </Border>

            <!-- 搜索栏 -->
            <Grid Grid.Row="1" Margin="14,10,14,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SearchBox" FontSize="12" Padding="8,6"
                         TextChanged="SearchBox_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background"
                                    Value="{DynamicResource AppSurfaceVariant}" />
                            <Setter Property="Foreground" Value="{DynamicResource AppText}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource AppBorder}" />
                            <Setter Property="BorderThickness" Value="1" />
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button Grid.Column="1" Content="刷新" Style="{DynamicResource SubtleButton}"
                        Margin="8,0,0,0" Padding="12,6" FontSize="11" Click="RefreshButton_Click" />
            </Grid>

            <!-- 窗口列表 -->
            <ListView Grid.Row="2" x:Name="WindowList" Margin="14,0,14,0"
                      SelectionMode="Single" MouseDoubleClick="WindowList_MouseDoubleClick"
                      Background="{DynamicResource AppSurface}"
                      BorderBrush="{DynamicResource AppBorderSubtle}" BorderThickness="1"
                      Foreground="{DynamicResource AppText}" FontSize="11">
                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource AppSurface}" />
                        <Setter Property="Foreground" Value="{DynamicResource AppText}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource AppBorder}" />
                        <Setter Property="BorderThickness" Value="0,0,1,1" />
                        <Setter Property="Padding" Value="8,4" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                    </Style>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Foreground" Value="{DynamicResource AppText}" />
                        <Setter Property="Padding" Value="4,2" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="Bd" Background="Transparent"
                                            BorderBrush="Transparent" BorderThickness="0"
                                            Padding="{TemplateBinding Padding}">
                                        <GridViewRowPresenter VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{DynamicResource AppAccentHover}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{DynamicResource AppAccent}" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.Resources>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="进程名" Width="110"
                                        DisplayMemberBinding="{Binding ProcessName}" />
                        <GridViewColumn Header="窗口标题" Width="260"
                                        DisplayMemberBinding="{Binding Title}" />
                        <GridViewColumn Header="句柄" Width="90"
                                        DisplayMemberBinding="{Binding HandleText}" />
                    </GridView>
                </ListView.View>
            </ListView>

            <!-- 底部按钮 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="14,10,14,14">
                <Button Content="取消" Style="{DynamicResource SubtleButton}"
                        Width="70" Padding="0,6" FontSize="11" Click="CancelButton_Click" />
                <Button Content="确定" Style="{DynamicResource AccentButton}"
                        Width="70" Padding="0,6" FontSize="11" Margin="8,0,0,0" Click="OkButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
