<Window x:Class="GameImpact.UI.Views.ScreenshotNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="保存截图" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Border Background="{DynamicResource AppBackground}" CornerRadius="8"
            BorderBrush="{DynamicResource AppBorder}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="{DynamicResource AppSurface}" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="保存截图" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource AppText}"
                               VerticalAlignment="Center" Margin="14,0,0,0" />
                    <Button Style="{DynamicResource WindowCloseButton}"
                            HorizontalAlignment="Right" Width="40" Height="40"
                            Click="CancelButton_Click">
                        <TextBlock Text="✕" FontSize="10" />
                    </Button>
                </Grid>
            </Border>

            <!-- 图片预览区域 -->
            <Border Grid.Row="1" Margin="14,10,14,10"
                    Background="{DynamicResource AppSurfaceVariant}"
                    BorderBrush="{DynamicResource AppBorder}" BorderThickness="1"
                    CornerRadius="4">
                <Image x:Name="PreviewImage" Stretch="Uniform"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>

            <!-- 文件名输入 -->
            <Grid Grid.Row="2" Margin="14,0,14,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="文件名：" FontSize="11" 
                           Foreground="{DynamicResource AppText}" Margin="0,0,0,6" />
                <TextBox x:Name="FileNameBox" Grid.Row="1" FontSize="12" Padding="8,6"
                         TextChanged="FileNameBox_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background"
                                    Value="{DynamicResource AppSurfaceVariant}" />
                            <Setter Property="Foreground" Value="{DynamicResource AppText}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource AppBorder}" />
                            <Setter Property="BorderThickness" Value="1" />
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Grid>

            <!-- 底部按钮 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"
                        Margin="14,0,14,14">
                <Button Content="取消" Style="{DynamicResource SubtleButton}"
                        Width="70" Padding="0,6" FontSize="11" Click="CancelButton_Click" />
                <Button Content="保存" Style="{DynamicResource AccentButton}"
                        Width="70" Padding="0,6" FontSize="11" Margin="8,0,0,0" 
                        Click="SaveButton_Click" x:Name="SaveButton" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
