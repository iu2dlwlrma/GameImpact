<Window x:Class="GameImpact.UI.Views.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:debug="clr-namespace:GameImpact.UI.Views.Debug"
        Title="调试面板" Height="500" Width="700"
        MinHeight="350" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <Border Background="{DynamicResource AppBackground}" CornerRadius="8"
            BorderBrush="{DynamicResource AppBorder}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="{DynamicResource AppSurface}" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" Margin="12,0,0,0"
                                VerticalAlignment="Center">
                        <TextBlock Text="🔧" FontSize="13" VerticalAlignment="Center" />
                        <TextBlock Text="调试面板" FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource AppText}" Margin="6,0,0,0"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <Button Style="{DynamicResource WindowCloseButton}" Click="Close_Click"
                            ToolTip="关闭" HorizontalAlignment="Right" VerticalAlignment="Stretch">
                        <TextBlock Text="✕" FontSize="11" />
                    </Button>
                </Grid>
            </Border>

            <!-- 调试面板内容 -->
            <TabControl x:Name="MainTabControl" Grid.Row="1" Background="Transparent"
                        BorderThickness="0"
                        Margin="10,6,10,10">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Foreground"
                                Value="{DynamicResource AppTextSecondary}" />
                        <Setter Property="Padding" Value="14,8" />
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="Border"
                                            Background="{TemplateBinding Background}"
                                            CornerRadius="6,6,0,0"
                                            Padding="{TemplateBinding Padding}"
                                            MouseLeftButtonDown="TabItem_MouseLeftButtonDown"
                                            MouseMove="TabItem_MouseMove"
                                            MouseLeftButtonUp="TabItem_MouseLeftButtonUp">
                                        <ContentPresenter ContentSource="Header"
                                                          HorizontalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource AppText}" />
                                            <Setter TargetName="Border"
                                                    Property="Background"
                                                    Value="{DynamicResource AppSurface}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border"
                                                    Property="Background"
                                                    Value="{DynamicResource AppSurfaceVariant}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <!-- 日志 Tab -->
                <TabItem Header="📋 日志">
                    <debug:LogTab x:Name="LogTabControl" />
                </TabItem>

                <!-- 预览 Tab -->
                <TabItem Header="🖼 预览">
                    <debug:PreviewTab x:Name="PreviewTabControl" />
                </TabItem>

                <!-- 坐标测试 Tab -->
                <TabItem Header="🎯 坐标" x:Name="CoordTab">
                    <debug:CoordinateTab x:Name="CoordinateTabControl" />
                </TabItem>
            </TabControl>
        </Grid>
    </Border>
</Window>
