<Window x:Class="GameImpact.UI.Views.GamePathSetupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置游戏路径" Height="180" Width="480"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Border Background="{DynamicResource AppBackground}" CornerRadius="8"
            BorderBrush="{DynamicResource AppBorder}" BorderThickness="1">
        <Grid Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="游戏根目录（请选择包含游戏主程序的文件夹）" FontSize="11"
                       Foreground="{DynamicResource AppText}" Margin="0,0,0,8" />

            <Grid Grid.Row="1" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PathBox" Grid.Column="0" FontSize="12" Padding="8,6"
                         VerticalContentAlignment="Center"
                         TextChanged="PathBox_TextChanged">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="{DynamicResource AppSurfaceVariant}" />
                            <Setter Property="Foreground" Value="{DynamicResource AppText}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource AppBorder}" />
                            <Setter Property="BorderThickness" Value="1" />
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button Grid.Column="1" Content="浏览" Style="{DynamicResource SubtleButton}"
                        Margin="8,0,0,0" Padding="12,6" FontSize="11" Click="BrowseButton_Click" />
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Style="{DynamicResource SubtleButton}"
                        Width="70" Padding="0,6" FontSize="11" Click="CancelButton_Click" />
                <Button x:Name="OkButton" Content="确定" Style="{DynamicResource AccentButton}"
                        Width="70" Padding="0,6" FontSize="11" Margin="8,0,0,0" Click="OkButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
